export const opcionesxRolXSelector = (rol = "eca") => {
  switch (rol) {
    case "eca":
      return [
         // {indice:}
      ];
    case "ecal":
      return [];
    default:
      return [];
  }
};
